naftiko: 1.0.0-alpha2
info:
  label: NHL API — NHL v3 Headshots
  description: 'NHL API — NHL v3 Headshots. 1 operations. Lead operation: Headshots. Self-contained Naftiko capability covering
    one Sportsdataio business surface.'
  tags:
  - Sportsdataio
  - NHL v3 Headshots
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SPORTSDATAIO_API_KEY: SPORTSDATAIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: nhl-nhl-v3-headshots
    baseUri: https://api.sportsdata.io
    description: NHL API — NHL v3 Headshots business capability. Self-contained, no shared references.
    resources:
    - name: v3-nhl-headshots-format-Headshots
      path: /v3/nhl/headshots/{format}/Headshots
      operations:
      - name: nhlv3headshotsheadshots
        method: GET
        description: Headshots
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: format
          in: path
          type: string
          description: Desired response format. Valid entries are <code>JSON</code> or <code>XML</code>.
          required: true
    authentication:
      type: apikey
      key: Ocp-Apim-Subscription-Key
      value: '{{env.SPORTSDATAIO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: nhl-nhl-v3-headshots-rest
    port: 8080
    description: REST adapter for NHL API — NHL v3 Headshots. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/v3/nhl/headshots/{format}/headshots
      name: v3-nhl-headshots-format-headshots
      description: REST surface for v3-nhl-headshots-format-Headshots.
      operations:
      - method: GET
        name: nhlv3headshotsheadshots
        description: Headshots
        call: nhl-nhl-v3-headshots.nhlv3headshotsheadshots
        with:
          format: rest.format
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: nhl-nhl-v3-headshots-mcp
    port: 9090
    transport: http
    description: MCP adapter for NHL API — NHL v3 Headshots. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: headshots
      description: Headshots
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: nhl-nhl-v3-headshots.nhlv3headshotsheadshots
      with:
        format: tools.format
      outputParameters:
      - type: object
        mapping: $.
